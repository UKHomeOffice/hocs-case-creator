ukvi-complaint.redrive.policy={"maxReceiveCount": "${ukvi-complaint.queue.maximumRedeliveries}", "deadLetterTargetArn":"arn:aws:sqs:${aws.sqs.region}:${aws.account.id}:${ukvi-complaint.queue.dlq.name}"}
ukvi-complaint.queue=aws-sqs://arn:aws:sqs:${aws.sqs.region}:${aws.account.id}:${ukvi-complaint.queue.name}?amazonSQSClient=#sqsClient&messageAttributeNames=All&redrivePolicy=${ukvi-complaint.redrive.policy}&waitTimeSeconds=20&backoffIdleThreshold=1&backoffMultiplier=${ukvi-complaint.queue.backOffMultiplier}&initialDelay=5000&delay=${poll.delay}
ukvi-complaint.dlq=aws-sqs://arn:aws:sqs:${aws.sqs.region}:${aws.account.id}:${audit.dlq.name}?amazonSQSClient=#sqsClient&messageAttributeNames=All